name: chat_scroll_challenge
description: "Interview challenge: Fix the auto-scroll behavior in a streaming chat UI."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  cross_cache: ^1.1.0
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_chat_core: ^2.9.0
  flutter_chat_ui: ^2.11.1
  flyer_chat_image_message: ^2.3.0
  flyer_chat_text_message: ^2.6.0
  flyer_chat_text_stream_message: ^2.3.0
  google_generative_ai: ^0.4.7
  image_picker: ^1.2.1
  provider: ^6.1.5+1
  uuid: ^4.5.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
